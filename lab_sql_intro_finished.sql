SELECT *
FROM actor;
SELECT *
from film;
SELECT *
from film_actor;
SELECT *
from category;
-- also clicked on the 'i' of the tables to explore the databases
SELECT title
from FILM;
SELECT name as 'language'
from language;
SELECT count(store_id)
from store;
-- there company has two stores
SELECT count(staff_id)
from staff;
-- the company has two employees
SELECT first_name
from staff;
-- The first names of the employees are Mike and Jon
